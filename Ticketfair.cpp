// Passes for Fair
// There is a fair going on in Chefland. Chef wants to visit the fair along with his 
// N friends. Chef manages to collect K passes for the fair. Will Chef be able to enter the fair with all his N friends?
// A person can enter the fair using one pass, and each pass can be used by only one person.

#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin>>t;
	while(t--){
	    int n,k;
	    cin>>n>>k;
	    if(n>=k){
	    cout<<"no"<<endl;
	    }
	    else {
	        cout<<"yes"<<endl;
	    }
	}

}